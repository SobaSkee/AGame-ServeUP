# Optional: full backend origin if not using Vite proxy (default empty = same origin + /api proxy)
# VITE_API_BASE=http://localhost:3001

# GitHub Pages project site only, CI sets this automatically.
# VITE_BASE_PATH=/AGame-ServeUP/