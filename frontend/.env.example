# Optional: full backend origin if not using Vite proxy (default empty = same origin + /api proxy)
# VITE_API_BASE=http://localhost:3001

# Required for saving pantry scans to MongoDB (24-char hex user _id from Atlas). Omit = detect only, no ingredientScans insert.
VITE_PANTRY_USER_ID=507f1f77bcf86cd799439011
